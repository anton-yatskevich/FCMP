import { ArticlesLocalFilterPipe } from './articles-local-filter.pipe';

describe('ArticlesLocalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArticlesLocalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
